@model IEnumerable<test.Models.StudyProcess>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kafedra.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Speciality.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kafedra.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Speciality.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudyProcessID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudyProcessID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudyProcessID })
        </td>
    </tr>
}

</table>
